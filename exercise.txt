Use the ls command from the root (/) directory to explore the directory structure ofhttps Linux. Move to each of the directories with the cd command and run pwd to verify whereyou are in the directory structure.
Use the whoami command to verify which user you are logged in as.
Use the locate command to find wordlists that can be used for password cracking.
Use the cat command to create a new file and then append to that file. Keep in mindthat > redirects input to a file and >> appends to a file.

Create a new directory called hackerdirectory and create a new file in that directory named hackedfile. Now copy that file to your /root directory and rename it secretfile.

How do you check your current location in the terminal?

What command do you use to find out your username (user) in Linux?

What is the difference between using cd and cd .. in the terminal?

How do you list the contents of a directory (folder) in Linux?

How can you see hidden files and directories in a Linux system?

Explain the difference between ls -la and ls -l commands.

What command do you use to get help when you are stuck in the terminal?

How do you find detailed information about a specific command in Linux?

What are the two commands that can be used to search for files or directories in the system, and what is the difference between them?

How do you create a new file using the terminal?

How do you delete a file using the terminal?

How do you create a new directory (folder) using the terminal?

How do you delete a directory (folder) using the terminal?

Explain the process of copying a file from one location to another in Linux.

How do you rename a file in Linux using the terminal?

